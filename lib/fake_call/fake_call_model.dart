class FakeCallModel {
  final String callerName;
  final String callerImage; // asset path
  final int callDuration; // in seconds

  FakeCallModel({
    required this.callerName,
    required this.callerImage,
    required this.callDuration,
  });
}
